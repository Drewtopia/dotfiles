{{- if .dev_computer -}}
# PowerShell ISE Profile for mise activation
# This sources the main PowerShell profile

$mainProfile = Join-Path (Split-Path $PROFILE) "Microsoft.PowerShell_profile.ps1"
if (Test-Path $mainProfile) {
    . $mainProfile
}
{{- end }}