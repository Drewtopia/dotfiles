# Global CLAUDE.md

## Environment

- **GitHub**: {{ .github.username }} (SSH: `git@github.com:{{ .github.username }}/<repo>.git`). Runtimes via mise; dotfiles via chezmoi.
{{- if .is_wsl }}
- This machine: WSL2 Ubuntu on Windows{{ if .work }} (corporate){{ end }}; interactive shell is zsh.
{{- else if eq .chezmoi.os "darwin" }}
- This machine: macOS; interactive shell is zsh.
{{- else if eq .chezmoi.os "windows" }}
- This machine: native Windows; shells are pwsh and Git Bash.
{{- else }}
- This machine: Linux; interactive shell is zsh.
{{- end }}
- Agent memory and rules live in the `~/.claude-vault/` repo, linked into `~/.claude/`.

## Guardrails — flag, don't force

Stop, say what and why, and let Drew decide before:

- committing or publishing anything that may contain a secret, credential, or `.env` file — check staged content; keep `.env` gitignored with a committed `.env.example`
- deleting beyond what was explicitly named
- weakening, skipping, or deleting an existing test to make it pass
- editing a governance surface (CI/workflows, ADRs, agent config) — go through `/edit-governance`

If Drew is away, leave the action undone and a note or tracker issue instead.

## Memory

Topic notes live in `~/.claude/memory/`, indexed by `memory.md`. They are not loaded by default: grep them when a task touches a listed tool or domain. Add durable knowledge to the matching topic file and keep the index current.
