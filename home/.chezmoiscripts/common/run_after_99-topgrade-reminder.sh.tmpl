{{- if ne .chezmoi.os "windows" -}}
{{- if .dev_computer -}}
#!/usr/bin/env bash
# Update work (app/tool/skill/marketplace upgrades) was moved out of
# `chezmoi apply` into topgrade. This reminder runs last so the prompt
# to update is the final thing shown.
echo ""
echo "[chezmoi] apply complete. Run 'topgrade' to update apps, tools, skills, and Claude marketplaces."
{{- end }}
{{- end }}
