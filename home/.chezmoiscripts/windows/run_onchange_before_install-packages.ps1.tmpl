#!/usr/bin/env pwsh

# Development Tools Installation for Windows
# PowerShell script to install development tools via curl and package managers

{{ if .dev_computer -}}
Write-Host "🪟 Installing development tools on Windows..." -ForegroundColor Green

function Test-Command($cmdname) {
    return [bool](Get-Command -Name $cmdname -ErrorAction SilentlyContinue)
}

# Development tools will be installed via mise after it's set up

# Mise (modern runtime version manager)
if (-not (Test-Command mise)) {
    Write-Host "📦 Installing Mise..." -ForegroundColor Blue
    try {
        irm https://mise.run | iex
    } catch {
        Write-Host "⚠️  Mise installation failed: $_" -ForegroundColor Yellow
    }
} else {
    Write-Host "✅ Mise already installed" -ForegroundColor Green
}

# Note: Proto removed - standardized on Mise as the primary runtime manager

# Atuin (shell history replacement)
if (-not (Test-Command atuin)) {
    Write-Host "📦 Installing Atuin..." -ForegroundColor Blue
    try {
        irm https://setup.atuin.sh | iex -ArgumentList '--no-modify-path'
        Write-Host "✅ Atuin installed successfully" -ForegroundColor Green
    } catch {
        Write-Host "⚠️  Atuin installation failed: $_" -ForegroundColor Yellow
    }
} else {
    Write-Host "✅ Atuin already installed" -ForegroundColor Green
}

# Install development tools via mise (from config.toml)
if (Test-Command mise) {
    Write-Host "📦 Installing development tools via mise..." -ForegroundColor Blue
    try {
        mise install
        Write-Host "✅ Development tools installed via mise" -ForegroundColor Green
    } catch {
        Write-Host "⚠️  Some mise tools failed to install: $_" -ForegroundColor Yellow
    }
} else {
    Write-Host "⚠️  Mise not found - cannot install development tools" -ForegroundColor Red
}

Write-Host "✅ Windows development tools installation completed!" -ForegroundColor Green
{{- else }}
Write-Host "🏠 Skipping development tools installation (not a dev computer)" -ForegroundColor Yellow
{{- end }}