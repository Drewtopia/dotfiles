# Pnpm globals — declared by name only. The reconciler adds missing ones as
# `<name>@latest` and removes undeclared ones (pnpm itself is always kept).
# Use topgrade or `pnpm update -g` to bump versions.
pnpm_globals:
  - "@openai/codex"
  - "@playwright/cli"
  - typescript
  - turbo
