# Agent skills installed via the `npx skills` CLI.
# Consumed by run_onchange_after_60-install-skills.sh.tmpl — when this
# file changes, the install script re-runs.
#
# Each entry: GitHub repo (owner/name) + the subset of skill names to
# install. Replaces the static $mattSkills list previously held in
# .chezmoiexternal.toml.tmpl, which went stale on upstream refactors.
skill_repos:
  - repo: mattpocock/skills
    # All 12 default-listed mattpocock skills (those returned by
    # `npx skills add mattpocock/skills -l`). Skills in subdirs that
    # the CLI hides — misc/, personal/, deprecated/, in-progress/ —
    # aren't installable via this flow.
    skills:
      # productivity/
      - grill-me
      - write-a-skill
      # engineering/
      - diagnose
      - grill-with-docs
      - improve-codebase-architecture
      - prototype
      - setup-matt-pocock-skills
      - tdd
      - to-issues
      - to-prd
      - triage
      - zoom-out
  - repo: vercel-labs/skills
    skills:
      - find-skills
  - repo: github/awesome-copilot
    # awesome-copilot has hundreds of skills; pull only the one we use.
    skills:
      - conventional-commit
