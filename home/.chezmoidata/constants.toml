[github]
    username = "Drewtopia"

# Global author identity. Static, not a 1Password lookup: it is public, and a
# vault lookup would break apply on machines without the personal vault.
[identity]
    name = "Drew"
    email = "drew@drewtopia.com.au"

[pwsh]
    # File under $env:POSH_THEMES_PATH, no extension.
    posh_theme = "spaceship"