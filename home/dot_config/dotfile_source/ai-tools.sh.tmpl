#!/usr/bin/env bash

# AI & Assistant Tools
# Claude, Gemini, GitHub Copilot, and other AI-powered development tools

## AI CLI Tools
# Most AI tools are installed globally and don't need PATH management
# They're typically available system-wide via npm global, homebrew, etc.

# AI tool aliases removed - user prefers no automatic aliases

## AI Tool Completions
if [[ -n ${ZSH_NAME} ]]; then
    # Add completions if available
    command -v claude >/dev/null 2>&1 && eval "$(claude completion zsh 2>/dev/null)" || true
elif [[ -n ${BASH} ]]; then
    command -v claude >/dev/null 2>&1 && source <(claude completion bash 2>/dev/null) || true
fi