# AGENTS.md

Agent-facing configuration for this chezmoi dotfiles repo. Domain vocabulary lives in `CONTEXT.md`.

## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues in `Drewtopia/dotfiles` (via the `gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical triage role strings, mapped 1:1 to GitHub labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` at the repo root. See `docs/agents/domain.md`.
