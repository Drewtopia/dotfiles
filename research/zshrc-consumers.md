# `.zshrc` / `.zprofile` consumers — ticket #45

Goal state: real `.zshrc` lives at `~/.config/zsh/.zshrc` (`$ZDOTDIR`), the
reverse symlink `home/dot_config/zsh/symlink_dot_zshrc.tmpl` (which today
points `~/.config/zsh/.zshrc` → `~/.zshrc`) is removed, and `~/.zshrc` stops
existing at all. Only `.zshenv` remains in `$HOME` (already exports
`ZDOTDIR="${XDG_CONFIG_HOME}/zsh"` — `home/dot_zshenv.tmpl:45`).

Confirmed on-disk today (`drew@` local machine, read-only check):
`~/.zshrc` is a real file (chezmoi-managed by `home/dot_zshrc.tmpl`);
`~/.config/zsh/.zshrc` is a symlink → `/Users/drew/.zshrc`
(`readlink` confirmed). This matches the ticket's description exactly.

## Table

| # | Consumer | Evidence | Status | Notes |
|---|----------|----------|--------|-------|
| 1 | `zshconfig` alias | `home/dot_zshrc.tmpl:123` — `alias zshconfig="nvim ~/.zshrc"` | **needs-change** | After migration `~/.zshrc` won't exist; opens/creates a stray, never-sourced file. Repoint to `${ZDOTDIR}/.zshrc` (or `$HOME/.config/zsh/.zshrc`). |
| 2 | `zshrc` alias | `home/dot_zshrc.tmpl:124` — `alias zshrc="source ~/.zshrc"` | **needs-change** | Same failure mode — `source` will error, file absent. Repoint to `${ZDOTDIR}/.zshrc`. |
| 3 | Reverse symlink itself | `home/dot_config/zsh/symlink_dot_zshrc.tmpl:1` — content `{{ .chezmoi.homeDir }}/.zshrc` | **needs-change (by design)** | This *is* the file the ticket wants removed. Migration = delete this template, move `dot_zshrc.tmpl` content to `home/dot_config/zsh/dot_zshrc.tmpl` (real file, no more `symlink_` indirection). |
| 4 | `.chezmoiignore.tmpl` Windows block | `home/.chezmoiignore.tmpl:98-99,107-108` — `.config/zsh`, `.config/zsh/**`, `.zprofile`, `.zshrc` all ignored, but only inside the `{{ if ne .chezmoi.os "windows" }} … {{ else }} … {{ end }}` (line 66) **else** branch | **safe** | This is Windows-only (PowerShell host, zsh never deployed there). Not gated on WSL — confirmed WSL (linux) deploys `.zshrc`/`.zprofile` identically to macOS, so the migration applies equally to WSL, no separate carve-out needed. No edit required; the whole `.config/zsh` dir (including the future real `dot_zshrc.tmpl`) stays correctly excluded on Windows. |
| 5 | `tool-functions` pnpm installer chmod guard | `home/.chezmoitemplates/tool-functions:34,38,42` — `chmod -w/+w ~/.zshrc ~/.zprofile ~/.zshenv` around the `get.pnpm.io/install.sh` curl-pipe | **unknown (dormant)** | Verified this template is **never invoked** anywhere in the repo (`grep -rn 'template "tool-functions"'` = no hits; not called from any `.chezmoiscripts/**` file). It's orphaned code. If it's ever wired back in: `~/.zshrc` will no longer exist post-migration (chmod on a missing path is already a silent no-op via `\|\| true`), but the *real* file at `~/.config/zsh/.zshrc` would need the same write-protection during the pnpm installer run — the guard doesn't cover it today. Flag for cleanup/removal or fix-if-reactivated, not urgent. |
| 6 | `run_onchange_after_15-pnpm-globals.sh.tmpl` comment | `home/.chezmoiscripts/common/run_onchange_after_15-pnpm-globals.sh.tmpl:26` — comment "PNPM_HOME from .zshrc doesn't propagate to chezmoi subshells on macOS" | **safe** | Comment-only; script re-exports `PNPM_HOME` itself unconditionally (line 27) regardless of where `.zshrc` lives. `PNPM_HOME` is actually set by `home/dot_config/shell/010-mise.sh.tmpl` (interactive-shell only), not by any literal `~/.zshrc` write — the propagation issue is about interactive-vs-noninteractive shells, not path location. No functional change needed; comment wording could be refreshed later for accuracy but isn't blocking. |
| 7 | `home/dot_config/cheatsheets/chezmoi` examples | Lines 51, 61, 102, 143 — `chezmoi add ~/.zshrc`, `chezmoi edit ~/.zshrc`, `chezmoi source-path ~/.zshrc`, `chezmoi cat ~/.zshrc` | **needs-change** | Illustrative CLI examples in a navi/cheat-style cheatsheet. After migration these example invocations target a path chezmoi no longer manages (the real target becomes `~/.config/zsh/.zshrc`). Low severity (docs, not executed), but should be updated so copy-pasting the example doesn't silently no-op. |
| 8 | `docs/github-auth-architecture.md:178` | "`.zshrc.tmpl` now exports `GITHUB_TOKEN` from `op://<vault>/GitHub PAT/token`…" | **needs-change** | Pre-existing drift found independent of this ticket: `GITHUB_TOKEN` is actually exported by `home/dot_config/shell/private_015-vault.sh.tmpl` (verified via grep), not directly in `dot_zshrc.tmpl`. The filename reference is doubly stale after migration since `dot_zshrc.tmpl` itself relocates to `home/dot_config/zsh/dot_zshrc.tmpl`. Doc-only, no runtime impact. |
| 9 | `docs/chezmoi-patterns-guide.md` (multiple, e.g. lines 958-1087) | Design/research doc dated 2026-03-25 describing "Current Setup" path-management layers referencing `.zshrc` line numbers, and community patterns | **safe** | This is a historical/aspirational planning document, already acknowledged stale by its own content (it predates `.zshenv`/`ZDOTDIR` — literally says "No `.zshenv` — everything loads in `.zshrc`", which is no longer true today even before this ticket's migration). Not a live consumer; no runtime dependency. Optional: note migration outcome here if/when the doc gets revisited, but out of scope for #45. |
| 10 | `README.md:36` | "For secrets used in `.zshrc`, SSH keys, etc., sign in with: `eval $(op signin)`" | **safe** | Prose reference, not a literal path dependency. Still accurate post-migration (secrets still load via the file, just relocated) — no edit required, purely cosmetic if touched at all. |
| 11 | `install.sh:10` | Comment: "apply-time templates that need it (zshrc, ssh keys, etc.)" | **safe** | Generic prose, no literal `~/.zshrc` path or behavior dependency. |
| 12 | `home/.chezmoitemplates/topgrade.toml` | Line 18 comment only ("oh-my-zsh managed via chezmoi externals…") | **safe** | No `[zsh]` step or literal path reference to `~/.zshrc`/`~/.zprofile` in the topgrade config itself. |
| 13 | VS Code integrated terminal | Web-verified: [VS Code Shell Integration docs](https://code.visualstudio.com/docs/terminal/shell-integration), [microsoft/vscode#148635](https://github.com/microsoft/vscode/pull/148635) (adds support for custom `ZDOTDIR` set in `~/.zshenv` when shell integration is on), [microsoft/vscode#157128](https://github.com/microsoft/vscode/issues/157128), [microsoft/vscode#250848](https://github.com/microsoft/vscode/issues/250848) (open issue: `.zshenv` executes but env vars from a custom `ZDOTDIR` sometimes don't stick on recent macOS builds) | **unknown** | When shell integration is enabled, VS Code sources `~/.zshenv` → `~/.zprofile` (if login) → `~/.zshrc` in `ZDOTDIR`-aware order, and there's explicit upstream work to respect a `ZDOTDIR` exported from `.zshenv`. But there's an open, unresolved edge-case bug (#250848) where this doesn't always take effect. Recommend manually verifying VS Code's integrated terminal actually loads the new `~/.config/zsh/.zshrc` after the migration lands, rather than assuming it just works. |
| 14 | `mise` install/shell activation | Web-verified: [mise.run/zsh](https://mise.run/zsh) installer appends `eval "$(mise activate zsh)"` to `"${ZDOTDIR:-$HOME}/.zshrc"`, guarded by a marker comment | **safe** | Explicitly `ZDOTDIR`-aware. Since `.zshenv` exports `ZDOTDIR` before any interactive-shell-triggered installer run, mise will correctly target `~/.config/zsh/.zshrc` post-migration. (Also moot in practice — this repo's `home/dot_config/shell/010-mise.sh.tmpl` already handles mise activation via the numbered shell-loader, not mise's own installer-appended line.) |
| 15 | `atuin` install.sh | Web-verified: atuin's `install.sh` appends `eval "$(atuin init zsh)"` to `"${ZDOTDIR:-$HOME}/.zshrc"` only if not already present | **safe** | Explicitly `ZDOTDIR`-aware, same reasoning as mise. |
| 16 | `pnpm` (`get.pnpm.io/install.sh` → `pnpm setup --force`) | WebFetch of the installer confirms it delegates rc-file selection to `pnpm setup`, not the shell wrapper itself. Web search surfaced historical pnpm issues where `pnpm setup` looked for `.zshrc` at a fixed/default location and **failed when the file didn't exist** ([pnpm#4027](https://github.com/pnpm/pnpm/issues/4027), [pnpm#5103](https://github.com/pnpm/pnpm/issues/5103)) | **needs-change / risk** | Real risk if Drew (or the dormant `tool-functions:install_pnpm`, see row 5) ever runs the official pnpm installer by hand post-migration: `~/.zshrc` will not exist at all (no file, no symlink), and `pnpm setup`'s historical behavior in that situation has been to either error or create a stray `~/.zshrc` that zsh never sources (since `ZDOTDIR` redirects zsh's own lookup to `~/.config/zsh/.zshrc`). Not currently exercised by any active chezmoi script — this repo manages `PNPM_HOME` itself via `home/dot_config/shell/010-mise.sh.tmpl` — but worth a manual smoke-test of the official installer post-migration, or a written note warning against running it directly. |
| 17 | Local override files (`~/.zshrc.local`, `~/.zshenv.local`, `~/.dotfiles.local`) | `home/dot_zshrc.tmpl:158-159` sources `${HOME}/.zshrc.local` and `${HOME}/.dotfiles.local`; `home/dot_zshenv.tmpl` (bottom) sources `${HOME}/.zshenv.local` | **safe** | These are deliberately `$HOME`-anchored (unmanaged, machine-specific escape hatches), not `$ZDOTDIR`-relative, and the migration doesn't change that design — `.zshenv` itself stays in `$HOME`, so `.zshenv.local` is unaffected. `.zshrc.local`/`.dotfiles.local` sourcing lines live *inside* `.zshrc`, which moves with it to `$ZDOTDIR`, but the paths they source (`$HOME/.zshrc.local`, `$HOME/.dotfiles.local`) are unchanged by design and still resolve correctly. On-disk check: `~/.zshenv.local` exists and is in active use (contains a `wslup()` helper); `~/.zshrc.local` and `~/.dotfiles.local` do not currently exist. No action needed. |
| 18 | `wezterm`, `tmux` shell invocation | Checked `~/.config/wezterm` (absent on this machine — not installed/used) and `~/.config/tmux/tmux.conf` / `~/.tmux.conf` for `default-command`/`default-shell` | **safe** | No wezterm config present locally. tmux config has no `default-command`/`default-shell` override referencing `.zshrc`/`.zprofile` by literal path — tmux just launches the user's login shell (`$SHELL`), which reads `.zshenv` → resolves `ZDOTDIR` → reads `.zshrc` from there. Unaffected by the migration. |

## Needs-change summary (action items)

1. `home/dot_zshrc.tmpl:123-124` — `zshconfig`/`zshrc` aliases: repoint from `~/.zshrc` to `${ZDOTDIR}/.zshrc`.
2. `home/dot_config/zsh/symlink_dot_zshrc.tmpl` — delete; move `home/dot_zshrc.tmpl` → `home/dot_config/zsh/dot_zshrc.tmpl` (drop the `symlink_` indirection, this file becomes the real target).
3. `home/dot_config/cheatsheets/chezmoi` — update the four `~/.zshrc` example invocations to `~/.config/zsh/.zshrc`.
4. `docs/github-auth-architecture.md:178` — fix stale `.zshrc.tmpl` → `GITHUB_TOKEN` source reference (already inaccurate pre-migration; doubly so after the file relocates).
5. `home/.chezmoitemplates/tool-functions:34,38,42` — dormant/orphaned `install_pnpm` chmod guard: either remove (if truly dead) or, if reactivated, extend the write-protection to cover `~/.config/zsh/.zshrc` instead of `~/.zshrc`.

## Unknowns worth a manual check after the migration lands

- VS Code integrated terminal: confirm it actually sources the relocated `~/.config/zsh/.zshrc` (open upstream bug microsoft/vscode#250848 re: custom `ZDOTDIR` from `.zshenv` not always taking effect).
- Official `pnpm` installer (`get.pnpm.io/install.sh` → `pnpm setup`): smoke-test or explicitly avoid running it by hand post-migration, given pnpm's history of hardcoding/mishandling the rc-file path when `~/.zshrc` doesn't exist (pnpm#4027, pnpm#5103).
